(function () {
  'use strict';

  // Nội dung demo Webtest 34. Bản thật sẽ do máy chủ cấp theo phiên; bản demo
  // dùng thẳng TERM_TEST_DEMO_CONTENT (mẫu giả lập, không chứa đáp án thật).
  window.WEBTEST34_CONTENT = Object.freeze(window.TERM_TEST_DEMO_CONTENT);
}());
